# HANDOFF.md — 当前开发状态交接

> 最后更新：2026-08-03。供下一次开发会话接手时阅读。

## 已经完成的功能

- ✅ 完整 No-Limit Texas Hold'em 状态机（PRE_FLOP → FLOP → TURN → RIVER → SHOWDOWN）
- ✅ 牌型判断：同花顺→高牌全部 10 种，含轮子顺子（A-2-3-4-5）、皇家同花顺
- ✅ 7 选 5 最佳手牌自动计算
- ✅ 底池/边池计算、平局分池（含奇数余数分配）
- ✅ 庄家按钮顺时针轮转、盲注（SB/BB）
- ✅ 六种动作：fold / check / call / raise / allin
- ✅ 下注轮次完成判断（下注额一致 + 均完成行动）
- ✅ 智能 AI 对手（Monte Carlo 胜率模拟 + 底池赔率决策 + 思考延迟）
- ✅ 多 AI 独立调度（每个 AI 独立计时器，避免死锁）
- ✅ 房间管理：4 位房间码、创建/加入、房主权限
- ✅ 局域网联机（同一 WiFi）
- ✅ 公网联机（Cloudflare 快速隧道，任意网络）
- ✅ **无限记账**：筹码输光后继续参与、显示负数欠款、赢牌自动还债
- ✅ **不自动补 AI**：房主手动添加，2 人真人即可开局
- ✅ PWA：manifest + Service Worker，可安装、离线缓存应用壳
- ✅ 一键启动脚本 `start-online.bat`（服务器 + 公网隧道）
- ✅ 27 项游戏逻辑单元测试（`npm test` 全过）

## 正在开发的功能

- 无（P1 大盲注加注权已修复，当前处于维护期）

## 尚未完成的功能

- ❌ 手机端 UI 优化（操作按钮、牌桌在小屏上的布局）
- ❌ 音效、动效（发牌/下注/赢牌）
- ❌ 断线后玩家重连恢复（当前重连后需要重新建房）
- ❌ 局间统计（每手输赢记录、玩家总盈亏）
- ❌ 自定义房间设置 UI（盲注/买入/最大人数，当前是代码内默认值）

## 已知 Bug

### P1：翻牌前大盲注加注权（BB option）— ✅ 已修复 2026-08-03
- **修复内容**：`startBettingRound()` PRE_FLOP 分支不再把大盲标记为 `acted`，大盲在翻牌前保留补行动权（可加注/跟注/弃牌），无人加注时 check 结束翻牌前
- **附带修复**：`performAction` 的 `raise`/`allin` 分支漏设 `p.acted = true`，导致大盲（或任何玩家）加注后轮次无法以"全员已行动"结束
- **验证**：新增 4 项单测（现共 31 项），WS 冒烟确认大盲被轮到行动

### 摊牌后自动连发下一局 — ✅ 已修复 2026-08-03
- **现象**：一局获胜后点"继续"自动立刻发下一局，无限循环（筹码保留）
- **修复**：`app.js` 的 `btn-continue` 改为二次确认——房主第一次点击按钮文字变为"确认开始下一局？"，3 秒内再次点击才发牌；非房主点击只关弹窗
- **验证**：WS 冒烟确认 hand_ended 后等待 3.2s 无自动发牌；纯前端交互，已通过 `npm run check`

### P2：无限记账下的无限加注
- **现象**：两方都无限 raise 时牌局不收敛（真实扑克中一方会认输）
- **性质**：设计决策的副作用，非崩溃 bug，但需要确认是否要加下注上限

### P2：公网隧道地址每次变化
- **现象**：Cloudflare 免费隧道每次重启生成新 URL
- **影响**：每次开房需重新发地址给朋友

### P2：无限记账下的无限加注
- **现象**：两方都无限 raise 时牌局不收敛（真实扑克中一方会认输）
- **性质**：设计决策的副作用，非崩溃 bug，但需要确认是否要加下注上限

### P2：公网隧道地址每次变化
- **现象**：Cloudflare 免费隧道每次重启生成新 URL
- **影响**：每次开房需重新发地址给朋友

## 当前技术债务

1. **`allin` 语义悬空**：无限记账下"全下"无真实含义，当前实现是 min-raise 推高，未来若要恢复真实 All-in 需重构
2. **前端无状态管理**：`app.js` 用全局 `state` 对象 + 直接 DOM 操作，规模增大后难维护
3. **房间配置硬编码**：盲注/买入/最大人数在 `Room` 构造时有默认值，但前端没有对应 UI
4. **测试覆盖不完整**：暂无 WS 消息协议层测试、AI 决策测试、断线重连测试

## 下一步最适合做的任务

1. **补充下注逻辑的回归测试**（大盲加注路径已测，可补翻牌后/转牌/河牌的加注循环测试）
2. 补 WS 协议层测试（房间创建/加入/动作流转）
3. AI 决策测试（`server/ai.js` 的 `decideAction` 各分支）

## 最近一次修改涉及的文件

| 文件 | 改动 |
|------|------|
| `server/dealer.js` | **P1 修复**：大盲翻牌前保留加注权（BB option）、`raise`/`allin` 补设 `acted` |
| `public/app.js` | **修复自动连发**：`btn-continue` 二次确认才发下一局 |
| `test/game.test.js` | 新增 4 项 BB option 测试（现 31 项） |
| `server/dealer.js` | 无限记账（负数不淘汰）、大盲注恢复逻辑、raise/allin 无上限 |
| `server/ai.js` | AI 决策适配负数筹码 |
| `server/server.js` | 去掉自动补 AI、AI minRaiseTo 修正、启动提示 |
| `public/app.js` | 负数筹码红色显示 + 欠款标签、加注滑块适配 |
| `public/style.css` | 欠款样式 |
| `public/sw.js` | 缓存版本 v1→v2 |
| `package.json` | 补 test/check 脚本 |
| `test/game.test.js` | 新增（27 项测试） |
| `README.md` / `CLAUDE.md` / `HANDOFF.md` / `TODO.md` / `CHANGELOG.md` | 本次整理新增 |
