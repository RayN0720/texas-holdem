# HANDOFF.md — 当前开发状态交接

> 最后更新：2026-08-03。供下一次开发会话接手时阅读。

## 已经完成的功能

- ✅ 完整 No-Limit Texas Hold'em 状态机（PRE_FLOP → FLOP → TURN → RIVER → SHOWDOWN）
- ✅ 牌型判断：同花顺→高牌全部 10 种，含轮子顺子（A-2-3-4-5）、皇家同花顺
- ✅ 7 选 5 最佳手牌自动计算
- ✅ 底池/边池计算、平局分池（含奇数余数分配）
- ✅ 庄家按钮顺时针轮转、盲注（SB/BB）
- ✅ 六种动作：fold / check / call / raise / allin
- ✅ 下注轮次完成判断（下注额一致 + 均完成行动）
- ✅ 智能 AI 对手（Monte Carlo 胜率模拟 + 底池赔率决策 + 思考延迟）
- ✅ 多 AI 独立调度（每个 AI 独立计时器，避免死锁）
- ✅ 房间管理：4 位房间码、创建/加入、房主权限
- ✅ 局域网联机（同一 WiFi）
- ✅ 公网联机（Cloudflare 快速隧道，任意网络）
- ✅ **无限记账**：筹码输光后继续参与、显示负数欠款、赢牌自动还债
- ✅ **不自动补 AI**：房主手动添加，2 人真人即可开局
- ✅ PWA：manifest + Service Worker，可安装、离线缓存应用壳
- ✅ 一键启动脚本 `start-online.bat`（服务器 + 公网隧道）
- ✅ 27 项游戏逻辑单元测试（`npm test` 全过）

## 正在开发的功能

- 无（当前处于整理期）

## 尚未完成的功能

- ❌ 翻牌前大盲注加注选项（见"已知 Bug"）
- ❌ 手机端 UI 优化（操作按钮、牌桌在小屏上的布局）
- ❌ 音效、动效（发牌/下注/赢牌）
- ❌ 断线后玩家重连恢复（当前重连后需要重新建房）
- ❌ 局间统计（每手输赢记录、玩家总盈亏）
- ❌ 自定义房间设置 UI（盲注/买入/最大人数，当前是代码内默认值）

## 已知 Bug

### P1：翻牌前大盲注无法加注
- **现象**：翻牌前所有玩家下注额一致后直接翻牌，无法持续加注
- **根因**：`dealer.js` 中盲注玩家（尤其大盲）在 `startBettingRound` 的 PRE_FLOP 恢复逻辑里被标记 `acted = true`，导致其被当作"已完成行动"，`requestAction` 不会为其提供加注选项，或大盲没有机会在翻牌前行使加注权
- **相关代码**：`dealer.js` 的 `startBettingRound()`（PRE_FLOP 分支约 141-151 行）、`requestAction()`（约 160-174 行）
- **影响**：翻牌前缺少一轮真实加注权，尤其大盲位玩家无法抢主动
- **建议修复**：大盲注玩家在 PRE_FLOP 仍有加注/弃牌/跟注权（BB option），需在所有其他玩家都行动后给大盲一次补行动的机会；只有当前下注未变化（无人加注）时大盲才可"check"

### P2：无限记账下的无限加注
- **现象**：两方都无限 raise 时牌局不收敛（真实扑克中一方会认输）
- **性质**：设计决策的副作用，非崩溃 bug，但需要确认是否要加下注上限

### P2：公网隧道地址每次变化
- **现象**：Cloudflare 免费隧道每次重启生成新 URL
- **影响**：每次开房需重新发地址给朋友

## 当前技术债务

1. **盲注/加注逻辑耦合**：`startBettingRound` 的 PRE_FLOP 恢复逻辑（`prevRound` 数组）脆弱，与 P1 bug 直接相关
2. **`allin` 语义悬空**：无限记账下"全下"无真实含义，当前实现是 min-raise 推高，未来若要恢复真实 All-in 需重构
3. **前端无状态管理**：`app.js` 用全局 `state` 对象 + 直接 DOM 操作，规模增大后难维护
4. **房间配置硬编码**：盲注/买入/最大人数在 `Room` 构造时有默认值，但前端没有对应 UI
5. **测试覆盖不完整**：暂无 WS 消息协议层测试、AI 决策测试、断线重连测试

## 下一步最适合做的任务

1. **修复 P1 bug（大盲注加注权）**——用户已明确报告，且有一份标准规则参考
2. 为修复后的下注逻辑补充针对性测试
3. 补 WS 协议层测试（房间创建/加入/动作流转）

## 最近一次修改涉及的文件

| 文件 | 改动 |
|------|------|
| `server/dealer.js` | 无限记账（负数不淘汰）、大盲注恢复逻辑、raise/allin 无上限 |
| `server/ai.js` | AI 决策适配负数筹码 |
| `server/server.js` | 去掉自动补 AI、AI minRaiseTo 修正、启动提示 |
| `public/app.js` | 负数筹码红色显示 + 欠款标签、加注滑块适配 |
| `public/style.css` | 欠款样式 |
| `public/sw.js` | 缓存版本 v1→v2 |
| `package.json` | 补 test/check 脚本 |
| `test/game.test.js` | 新增（27 项测试） |
| `README.md` / `CLAUDE.md` / `HANDOFF.md` / `TODO.md` / `CHANGELOG.md` | 本次整理新增 |
